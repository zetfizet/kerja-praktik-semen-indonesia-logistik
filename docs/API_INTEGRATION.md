# 📖 API Integration & Data Sources\n\n**External APIs and data source documentation**\n\n---\n\n## 📑 Table of Contents\n\n- [BMKG Weather API](#bmkg-weather-api)\n- [DEVOM Warehouse Connection](#devom-warehouse-connection)\n- [Data Source Reliability](#data-source-reliability)\n- [Integration Best Practices](#integration-best-practices)\n\n---\n\n## BMKG Weather API\n\n### Overview\n\n**Provider:** BMKG (Badan Meteorologi, Klimatologi, dan Geofisika - Indonesian Met. Agency)  \n**Type:** Public REST API  \n**License:** Open / Public Data  \n**Update Frequency:** Every 2 hours  \n**Coverage:** Indonesia-wide (multiple cities/locations)  \n**Data Retention:** Real-time + 30-day history (in warehouse)\n\n### Endpoints\n\n#### Latest Data\n\n```http\nGET https://api.bmkg.go.id/latest/weather\n```\n\n**Response Format:** JSON\n\n```json\n{\n  \"status\": \"success\",\n  \"data\": [\n    {\n      \"id\": \"5001\",\n      \"location\": \"Jakarta\",\n      \"coordinate\": [\"6.2083\", \"106.8456\"],\n      \"current\": {\n        \"weather\": \"Mendung\",\n        \"temp_c\": 28.5,\n        \"temp_f\": 83.3,\n        \"humidity\": \"72%\",\n        \"wind_speed\": 4.2,\n        \"wind_direction\": \"Timur\",\n        \"wind_direction_deg\": 90,\n        \"pressure\": 1012,\n        \"uvi\": 5.2,\n        \"visibility\": 10.0,\n        \"dewpoint_c\": 23.1\n      },\n      \"last_updated\": \"2026-04-13T10:00:00+07:00\"\n    }\n  ]\n}\n```\n\n#### Forecast Data\n\n```http\nGET https://api.bmkg.go.id/latest/forecast\n```\n\n**Returns:** 5-day forecast data with hourly granularity\n\n### Available Locations\n\n**Indonesia Coverage:**\n\n```\nJava Island:\n  - Jakarta (6.2083, 106.8456)\n  - Bandung (6.9175, 107.6062)\n  - Surabaya (-7.2575, 112.7521)\n  - Semarang (-6.9674, 110.4185)\n\nSumatra Island:\n  - Medan (3.5952, 98.6722)\n  - Palembang (-3.0052, 104.7463)\n  - Padang (-0.9492, 100.4172)\n\nBali & East:\n  - Denpasar (-8.6705, 115.2126)\n  - Makassar (-5.3520, 119.4327)\n\nOther regions:\n  [Total 50+ locations]\n```\n\n### Rate Limits\n\n```\nRequests per hour:    Unlimited (as of last check)\nTimeout:              30 seconds per request\nMax payload:          ~5MB per response\nConcurrency:          Unlimited\n```\n\n### Error Handling in DAG\n\n```python\ndef fetch_weather_data():\n    try:\n        response = requests.get(\n            'https://api.bmkg.go.id/latest/weather',\n            timeout=30\n        )\n        if response.status_code == 200:\n            return response.json()['data']\n        else:\n            # API error\n            raise Exception(f\"API returned {response.status_code}\")\n    \n    except requests.Timeout:\n        # Network timeout - will retry via DAG retry mechanism\n        raise Exception(\"API timeout\")\n    \n    except Exception as e:\n        # Other errors\n        logger.warning(f\"Weather API error: {str(e)}\")\n        return []  # Return empty, continue pipeline\n```\n\n### Caching Strategy\n\n```python\n# Prevent duplicate data insertion\nINSERT INTO weather.fact_weather_hourly (...)\nVALUES (...)\nON CONFLICT (location, timestamp_hour) DO NOTHING;\n```\n\n**Deduplication occurs when:**\n- Same location\n- Same hour\n- Identical temperature/humidity/wind values\n\n---\n\n## DEVOM Warehouse Connection\n\n### Connection Details\n\n```\nHost:           devom.silog.co.id\nPort:           5432 (PostgreSQL default)\nDatabase:       om\nUser:           om\nPassword:       om\nSSL/TLS:        Standard PostgreSQL protocol\n```\n\n### Access Method\n\n**Direct Connection:**\n```bash\npsql -h devom.silog.co.id -U om -d om -p 5432\n```\n\n**From Docker Container:**\n```bash\ndocker-compose exec airflow-scheduler bash\npsql -h devom.silog.co.id -U om -d om -p 5432\n```\n\n### Database Structure\n\n**Schemas (if multi-schema):**\n```sql\n-- List schemas\nSELECT schema_name FROM information_schema.schemata;\n```\n\n**Primary Tables (~90 tables):**\n- `drivers` - Driver/personnel info\n- `armada` - Vehicle fleet data\n- `perjalanan` - Trip/journey records\n- `pengiriman` - Delivery assignments\n- `customers` - Client information\n- `invoices` - Invoice records\n- `payments` - Payment transactions\n- `[... 80+ more tables]\n\n### Data Volume\n\n```\nEstimated size:       2-5GB\nRow count (total):    500K-2M\nLargest tables:       perjalanan, activity_log, transactions\nDaily growth:         10-100MB\n```\n\n### Sync Strategy\n\n**Full Sync (Current):**\n```python\n# Copy all data from source to warehouse\n# No incremental logic\n# Pros: Simple, reliable, complete\n# Cons: Slower, network intensive\n\nfor table in source_tables:\n    source_data = SELECT * FROM source.table\n    INSERT INTO warehouse.table VALUES (...)\n    COMMIT\n```\n\n**Potential Improvement (Future):**\n```python\n# Incremental sync (if timestamps available)\n# Copy only rows updated since last sync\n# Pros: Much faster, less network traffic\n# Cons: Need updated_at column, more complex\n\nLAST_SYNC = get_last_sync_time(table_name)\nNEW_DATA = SELECT * FROM source.table \n           WHERE updated_at > LAST_SYNC\nINSERT INTO warehouse.table (...)\n```\n\n### Connection Issues & Solutions\n\n#### Common Problems\n\n```\nError: \"could not translate host name\"\nSolution: Check DNS, try IP address instead\n\nError: \"connection refused\"\nSolution: Verify devom.silog.co.id is reachable (ping)\n\nError: \"authentication failed\"\nSolution: Verify credentials (om/om)\n\nError: \"connection timeout\"\nSolution: Check network, may need VPN\n```\n\n---\n\n## Data Source Reliability\n\n### BMKG API Reliability\n\n**Uptime:** ~99.5% (based on operational monitoring)\n\n**Known Issues:**\n```\nMaintenance window:   Typically Sundays 00:00-02:00 WIB\nUpdate frequency:     Every 2 hours (may vary)\nData lag:             0-30 minutes typical\n```\n\n**Fallback Strategy:**\n```python\n# If BMKG fails, use previous data\nif fetch_from_bmkg_fails():\n    use_previous_hour_data()\n    mark_as_propagated()\n    log_warning(\"Using propagated data from BMKG API failure\")\n```\n\n### DEVOM Warehouse Reliability\n\n**Uptime:** ~99% (production database)\n\n**Known Issues:**\n```\nBackup window:        Daily 02:00-04:00 WIB (possible slowdown)\nPeak hours:           08:00-11:00 WIB (may be slower)\n```\n\n**Monitoring:**\n```sql\n-- Check DEVOM connectivity health\nSELECT \n    datname,\n    numbackends as active_connections,\n    xact_commit as transactions_committed,\n    xact_rollback as transactions_rolled_back\nFROM pg_stat_database\nWHERE datname = 'om';\n```\n\n---\n\n## Integration Best Practices\n\n### Error Handling\n\n**Retry Logic:**\n```python\nfrom tenacity import retry, stop_after_attempt, wait_fixed\n\n@retry(stop=stop_after_attempt(3), wait=wait_fixed(5))\ndef fetch_from_api():\n    response = requests.get(url, timeout=30)\n    response.raise_for_status()\n    return response.json()\n```\n\n**Graceful Degradation:**\n```python\n# If external source fails, continue with cached data\ntry:\n    new_data = fetch_from_source()\nexcept Exception as e:\n    logger.warning(f\"Failed to fetch new data: {e}\")\n    new_data = fetch_from_cache()  # Use previous data\n```\n\n### Monitoring & Alerting\n\n**Data Freshness Metrics:**\n```sql\nSELECT \n    location,\n    MAX(data_timestamp) as latest_data,\n    EXTRACT(EPOCH FROM (NOW() - MAX(data_timestamp)))/60 as age_minutes,\n    CASE \n        WHEN EXTRACT(EPOCH FROM (NOW() - MAX(data_timestamp)))/60 <= 60 THEN 'FRESH'\n        WHEN EXTRACT(EPOCH FROM (NOW() - MAX(data_timestamp)))/60 <= 180 THEN 'WARNING'\n        ELSE 'STALE'\n    END as freshness_status\nFROM weather.fact_weather_hourly\nGROUP BY location\nORDER BY age_minutes DESC;\n```\n\n**Alert Conditions:**\n```\nCondition 1: API unavailable for > 2 hours\n  → Alert severity: HIGH\n  → Notification: Email ops team\n\nCondition 2: Data stale for > 6 hours\n  → Alert severity: MEDIUM\n  → Notification: Log to monitoring system\n\nCondition 3: Error rate > 10% in last hour\n  → Alert severity: MEDIUM\n  → Notification: Trigger dashboard refresh\n```\n\n### Connection Pooling\n\n**Benefits:**\n```\n✅ Reduce connection overhead\n✅ Improve throughput\n✅ Better resource management\n✅ Prevent connection exhaustion\n```\n\n**Configuration:**\n```python\nfrom sqlalchemy import create_engine\n\nengine = create_engine(\n    'postgresql://postgres:postgres123@localhost:5433/warehouse',\n    pool_size=10,              # Connections to keep ready\n    max_overflow=20,           # Additional connections if needed\n    pool_recycle=3600,         # Recycle connections after 1 hour\n    pool_pre_ping=True,        # Verify connection before using\n)\n```\n\n### Data Validation\n\n**Validation Checks:**\n```python\ndef validate_weather_data(record):\n    \"\"\"Validate BMKG API data before storing\"\"\"\n    \n    # Check required fields\n    assert record.get('location'), \"Missing location\"\n    assert record.get('temp_c') is not None, \"Missing temperature\"\n    \n    # Check value ranges\n    assert -10 <= record['temp_c'] <= 50, \"Temperature out of range\"\n    assert 0 <= record.get('humidity', 0) <= 100, \"Humidity out of range\"\n    assert record.get('wind_speed', 0) >= 0, \"Wind speed invalid\"\n    \n    # Check timestamp\n    assert record.get('last_updated'), \"Missing timestamp\"\n    \n    return True\n```\n\n### Rate Limiting & Throttling\n\n**Implementation:**\n```python\nfrom datetime import datetime, timedelta\n\nclass APIThrottler:\n    def __init__(self, max_calls=100, time_window_seconds=3600):\n        self.max_calls = max_calls\n        self.time_window = time_window_seconds\n        self.calls = []\n    \n    def is_allowed(self):\n        now = datetime.now()\n        # Remove old calls outside window\n        self.calls = [c for c in self.calls \n                     if c > now - timedelta(seconds=self.time_window)]\n        \n        if len(self.calls) < self.max_calls:\n            self.calls.append(now)\n            return True\n        return False\n```\n\n---\n\n## Integration Testing\n\n### Test BMKG API\n\n```bash\n# Quick test from terminal\ncurl -s https://api.bmkg.go.id/latest/weather | jq '.data[0]'\n\n# Python test\npython3 << 'EOF'\nimport requests\nres = requests.get('https://api.bmkg.go.id/latest/weather', timeout=10)\nprint(f\"Status: {res.status_code}\")\nprint(f\"Records: {len(res.json()['data'])}\")\nEOF\n```\n\n### Test DEVOM Connection\n\n```bash\n# From localhost\npsql -h devom.silog.co.id -U om -d om -c \"SELECT COUNT(*) FROM drivers;\"\n\n# From Docker container\ndocker-compose exec airflow-scheduler \\\n  psql -h devom.silog.co.id -U om -d om -c \"SELECT COUNT(*) FROM drivers;\"\n```\n\n### Test Airflow Connections\n\n```bash\n# List configured connections\nairflow connections list\n\n# Test specific connection\nairflow connections test postgres_warehouse\n```\n\n---\n\n📖 **Next:** Review [Architecture Guide](ARCHITECTURE.md) for system integration details  \n👈 **Back to:** [Main README](../README.md)\n